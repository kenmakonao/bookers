<div class="row">
<%= render 'users/profile',user: @user,book: @book %>
<h1>Book detail</h1>
<div class="col-sm-9">
<table class="table table-hover table-inverse">
		<thead>
		<tr>
			<th></th><th></th><th></th><th colspan="2">
			</th>
		</tr>
	</thead>

	<tbody>
			<tr>
				<td><%= attachment_image_tag @books.user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "30x30" %><br><%= link_to @books.user.name ,user_path(@books.user)  %></td>
				<td><%= @books.title %></td>
				<td><%= @books.body %></td>
				<td><%= link_to "Edit", edit_book_path(@books),class:"btn-sm btn-success"%></td>
				<td><%=link_to "Destroy", book_path(@books), method: :delete, data: { confirm: 'Are you sure?' },class:"btn-sm btn-danger" %></td>
			</tr>
 	</tbody>
</table>
</div>
</div>
