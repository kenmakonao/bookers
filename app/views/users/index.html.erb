<div class="row">
<%= render 'users/profile',user: @user,book: @book %>


  <div class="col-sm-9">

<table class="table table-hover table-inverse">
		<thead>
		<tr>
			<th>image</th><th>name</th><th colspan="1">
			</th>
		</tr>
	</thead>

	<tbody>
		<% @users.each do |user| %>
			<tr>
				<td><%= attachment_image_tag @user, :profileimage,　format: 'jpeg', fallback: "no_image.jpg", size: "30x30" %></td>
				<td><%= user.name %></td>
				<td><%= link_to "show", user_path(user) %></td>
			</tr>
 		<% end %>
 	</tbody>
</table>
</div>
</div>

